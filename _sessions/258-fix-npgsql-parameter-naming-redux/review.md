# Review: #266 — Phase 9 (session 2)

## Scope
Diff range: 2767e77..HEAD (Phase 9 deltas — 6 commits, 30 files changed, 2597 insertions / 128 deletions).
Session-1 review (phases 1-8) was completed and classified previously. Its contents are preserved in git history at commit 2767e77's session dir state. This review pass is scoped to Phase 9 only.

## Classifications
| # | Section | Finding (one line) | Sev | Rec | Class | Action Taken |
|---|---------|--------------------|-----|-----|-------|--------------|
| 1 | Plan Compliance | Phase 9 implementation matches user directive (mirror Lite, no blanket skips) | info | D | D | (no action) |
| 2 | Plan Compliance | Three inline workarounds are not silent skips (each has #267/#268 reference) | info | D | D | (no action) |
| 3 | Plan Compliance | pr-body.md accurately documents Phase 9 DDL + triage decisions | info | D | D | (no action) |
| 4 | Correctness | Pg-execute assertions rely on positional row order without ORDER BY at 7+ sites (latent flake) | medium | A | A | Helper `SortedByAsync` extension on `Task<List<T>>` added in `src/Quarry.Tests/PgRowOrderExtensions.cs`. Applied at 11 sites: `CrossDialectSelectTests.Select_Distinct`, `CrossDialectNavigationJoinTests` (Where, GroupBy, DeepChain), `CrossDialectCteTests` (7 sites: FromCte_CapturedParam, FromCte_AllColumns, TwoChainedWiths_DistinctDtos, ThreeChainedWiths_AllUsedDownstream, TwoChainedWiths_FirstEmptySecondCaptured, FromCte_DedicatedDto). All 2996 tests green. |
| 5 | Correctness | DDL column types semantically match SqlTypeMapper.MapPostgres (cosmetic case difference only) | info | D | D | (no action) |
| 6 | Correctness | Seed values use PG-native literals (TRUE/FALSE, ISO timestamp strings) | info | D | D | (no action) |
| 7 | Correctness | Select_Distinct sort-then-assert workaround materialises correctly | info | D | D | (no action) |
| 8 | Correctness | Join_Distinct_OrderBy_Limit skip leaves AssertDialects intact (Pg SQL still asserted) | info | D | D | (no action) |
| 9 | Correctness | Cte rename orderCutoff→cutoff preserves the test's two-distinct-types intent | info | D | D | (no action) |
| 10 | Correctness | pg2 shadow-variable pattern in Subquery is structurally sound across all 40 sites | info | D | D | (no action) |
| 11 | Correctness | Update tests cleanly isolate Lite vs Pg side effects (separate connections + tx wrappers) | info | D | D | (no action) |
| 12 | Test Quality | Pg mirrors duplicate Lite's value assertions (no count-only weakening detected) | info | D | D | (no action) |
| 13 | Test Quality | Sort-then-assert pattern not abstracted into a shared helper | low | B | B | Helper added as part of #4 remediation. Extension on `Task<List<T>>` (not `PreparedQuery<T>`) so QRY036 chain analyzer still sees `.ExecuteFetchAllAsync()` as the literal terminal. |
| 14 | Test Quality | Comments referencing #267 and #268 detail the workaround condition + remediation path | info | D | D | (no action) |
| 15 | Test Quality | Phase 9 expands regression surface ~75x (parameter-binding regressions now fail loudly) | info | D | D | (no action) |
| 16 | Codebase Consistency | Pg result variable naming uniform (pgResults/pgResult/pgAffected/pgNewId/pg) | info | D | D | (no action) |
| 17 | Codebase Consistency | pg2 shadow-variable pattern consistent across 42 sites (Subquery + Cte + WindowFunction) | info | D | D | (no action) |
| 18 | Codebase Consistency | PostgresTestContainer doc comments updated to reflect Phase 9 DDL + FK rationale | info | D | D | (no action) |
| 19 | Codebase Consistency | workflow.md decisions accurately reflect what was committed | info | D | D | (no action) |
| 20 | Codebase Consistency | Double-checked-locking pattern unchanged from session 1 | info | D | D | (no action) |
| 21 | Integration | Phase 9 is test-only (no src/Quarry/ or src/Quarry.Generator/ changes) | info | D | D | (no action) |
| 22 | Integration | Test runtime within Phase 4 baseline (~20s for 2996 tests) | info | D | D | (no action) |
| 23 | Integration | No new CI infrastructure required beyond Phase 4's Docker dependency | info | D | D | (no action) |
| 24 | Integration | Session-archive issue bodies (#269/#270 drafts) are doc-only, not build-consumed | info | D | D | (no action) |


## Plan Compliance

| # | Finding | Severity | Why It Matters |
|---|---------|----------|----------------|
| 1 | Phase 9 was added in session 2 and is not in the original `plan.md`. The session-2 directive recorded in `workflow.md` ("Mirror Lite exactly — same assertions on both" + "None — attempt all, triage failures") is followed: 22 of 22 in-scope `CrossDialect*Tests` files received Pg-execute mirrors, ~233 mirror blocks total, no blanket skips. | info | Confirms Phase 9 implementation matches the user directive. Diagnostics-only file (`CrossDialectDiagnosticsTests.cs`) is correctly untouched per pr-body.md note (no `Execute*` calls to mirror). |
| 2 | Three inline test-level workarounds were added (`Select_Distinct` sort, `Join_Distinct_OrderBy_Limit` Pg execution skip, `Cte_TwoChainedWiths_DistinctDtos_CapturedParams` variable rename). All three carry comments referencing tracking issues #267 / #268 and explain the divergence in-place — none are silent skips. The `Join_Distinct_OrderBy_Limit` skip leaves the Pg SQL-text assertion intact via `AssertDialects` (verified — `QueryTestHarness.AssertDialects` at `src/Quarry.Tests/QueryTestHarness.cs:188-216` always asserts the Pg dialect string regardless of execution). | info | Triage is honest. The "no silent skips" directive holds. |
| 3 | The Phase 9 entry in `pr-body.md` (added in commits 6f9f83e, 613a4df) accurately documents the harness DDL changes (NUMERIC, BOOLEAN, TIMESTAMP/TIMESTAMPTZ, FK omission rationale) and the three triage decisions, including #267 and #268 follow-up references. | info | Reviewer-facing artifact is current. |

## Correctness

| # | Finding | Severity | Why It Matters |
|---|---------|----------|----------------|
| 4 | Multiple Pg-execute assertions rely on positional row order without an explicit `ORDER BY` in the chain. Examples: `CrossDialectNavigationJoinTests.cs:207-214` (`Pg.Orders().Where(...)` then `pgResults[0..2]` index-based assertion), `CrossDialectNavigationJoinTests.cs:358-364` (`NavigationJoin_DeepChain_ExecutesCorrectly` — three-level join with positional asserts), `CrossDialectCteTests.cs:109-112,193-198,266-273,338-343,403-408,464-469` (CTE-projected order tuples asserted by `pgResults[0]` / `pgResults[1]` against expected `(1, 250.00m)` / `(3, 150.00m)` pairs with no ORDER BY). PG does not guarantee row order from a base scan or a join without `ORDER BY`. The session-2 triage caught and fixed exactly this in `Select_Distinct` but did not generalise the inspection. The current 3314/3314 green suite passes because PG happens to return small-table sequential-scan results in heap order today; a planner change (statistics, parallel scan, hash join chosen for a CTE join) would surface as flake. | medium | Latent flake hazard. The session-2 fix template (`(await pg.ExecuteFetchAllAsync()).OrderBy(r => r.UserId).ToList()`) is straightforward, but applying it broadly was out of scope. Alternative: add `.OrderBy(...)` to the chain so Lite and Pg both order. |
| 5 | `PostgresTestContainer.CreateSchemaObjectsAsync` aligns column types with `Quarry.Migration.SqlTypeMapper.MapPostgres` for: `int` → `INTEGER` (✓), `bool` → `BOOLEAN` (✓ matches `MapPostgres "bool" => "boolean"`), `decimal` with precision → `NUMERIC(18, 2)` (✓ matches `numeric({precision},{scale})` shape), `DateTime` → `TIMESTAMP` (✓), `DateTimeOffset` → `TIMESTAMPTZ` (✓), `string` → `TEXT` (✓ matches when no length specified). One subtle deviation: `SqlTypeMapper.MapPostgres` emits lower-case `"integer"`/`"text"` etc., while the harness DDL emits upper-case `INTEGER`/`TEXT`. PG is case-insensitive on type names, so this is purely cosmetic. | info | Test harness DDL semantically matches what production migrations would emit. Cosmetic case difference is not a defect. |
| 6 | Seed values in `PostgresTestContainer.SeedDataAsync` correctly use PG-native literals: `TRUE`/`FALSE` for `BOOLEAN` columns (e.g., `users.IsActive`, `accounts.IsActive`) and bare timestamp string literals (`'2024-01-15 00:00:00'`) which PG implicitly casts to `TIMESTAMP` / `TIMESTAMPTZ`. No 1/0 integer-bool seed values found. The `events.ScheduledAt` / `CancelledAt` use full ISO with timezone offsets (`'2024-06-15 10:30:00+00:00'`) appropriate for `TIMESTAMPTZ`. | info | Seed data is consistent with the column types. |
| 7 | The `Select_Distinct` workaround at `CrossDialectSelectTests.cs:377` — `var pgResults = (await pg.ExecuteFetchAllAsync()).OrderBy(r => r.UserId).ToList();` — materialises correctly. The result type is the entity `User` (because `.Select(u => u)` projects the entity), `OrderBy(r => r.UserId)` returns `IOrderedEnumerable<User>`, `ToList()` produces `List<User>` which supports indexer assertions in the rest of the test. The asymmetry (Lite asserts on insertion-order `results`, Pg asserts on UserId-sorted `pgResults`) is justified by the inline comment and is the minimum-surface fix. | info | Workaround is sound. |
| 8 | The `Join_Distinct_OrderBy_Limit` skip at `CrossDialectCompositionTests.cs:452-459` correctly leaves `AssertDialects` intact (the SQL-text assertion runs for Pg via `pg.ToDiagnostics()`), so generator-output verification on Pg is preserved. Comment is precise (cites PG error 42P10 by code, names the cause and the necessary alternative). | info | Skip is honest and well-scoped. |
| 9 | The `Cte_TwoChainedWiths_DistinctDtos_CapturedParams` rename `orderCutoff` → `cutoff` at `CrossDialectCteTests.cs:231` preserves the test's stated intent. The summary docstring at lines 205-214 says the test validates "BOTH inner queries capture a local variable" — both `cutoff` (decimal) and `activeFilter` (bool) remain captured locals of distinct types, so the chained-With closure-merging behaviour (regression test for #206) is still exercised. The bug-explanation comment block at 221-230 names #268, names the failure mode (`MissingFieldException` at `.Prepare()`), and names the remediation condition for re-naming. | info | Rename does not erode test coverage; the docstring's two-distinct-types intent is preserved. |
| 10 | The `pg2` shadow-variable pattern in `CrossDialectSubqueryTests.cs` (40 occurrences) is structurally sound: each `pg2` re-builds the chain with `.Select(u => (u.UserId, u.UserName))` so the result type is `(int, string)` — matching `lt`'s tuple projection, while the original `pg` retains the entity-only projection that the `AssertDialects` SQL-text assertion was originally written against. Sample-checked sites at lines 41-45 (`Where_Any_Parameterless`), 514-519 (`Where_OrderDate_Max_Cutoff` with captured `cutoff` DateTime), 1338-1344 (aggregate Pg projection with `Select` re-applied) all show consistent shape: `pg2` projection mirrors `lt` projection, assertions are applied to `pgResults` from `pg2`. No collisions: `pg2` is method-scoped, never escapes a single `[Test]` body. | info | Pattern is consistent and produces meaningful coverage. The original `pg` variable is left in place to preserve the SQL-text assertion semantics. |
| 11 | Update tests (`CrossDialectUpdateTests.cs`) execute on both Lite and Pg connections within the same harness instance. Each connection has its own seed (Lite via in-memory SQLite, Pg via baseline schema + transaction wrapper), so a `lt.ExecuteNonQueryAsync()` UPDATE followed by `pg.ExecuteNonQueryAsync()` UPDATE on the same `UserId == 1` predicate operates on independent rows. Read-back queries (e.g., `Lite.Users().Where(u => u.UserId == 1).Select(u => u.UserName).ExecuteFetchFirstAsync()` followed by `Pg.Users()...`) correctly read from their respective databases. No cross-pollution. | info | Test isolation per connection is preserved. |

## Security

No concerns.

## Test Quality

| # | Finding | Severity | Why It Matters |
|---|---------|----------|----------------|
| 12 | All sampled Pg mirror blocks duplicate Lite's specific value assertions, not just count. Spot-check: `CrossDialectAggregateTests.cs` adds `pgShipped.Item2 == 2` and `pgShipped.Item3 == 400.00m` (matching Lite); `CrossDialectStringOpTests.cs:60-62` adds `pgResults[0].UserName == "Alice"` (matching Lite); `CrossDialectComplexTests.cs:67-71` adds `pgResults[0] == ("Alice", "alice@test.com")` (matching Lite). No "count-only" weakening was found in the sampled diff. The two cases where a Lite assertion is bare `Has.Count.EqualTo(1)` (`CrossDialectStringOpTests.cs:329-330,355-356`) the Pg mirror is also bare `Has.Count.EqualTo(1)` — symmetric, not weaker. | info | Mirror is faithful. |
| 13 | The `Select_Distinct` sort-then-assert pattern is documented in-place but is not abstracted into a shared helper. If a future test needs the same pattern, the developer either copies the comment or risks dropping it. Low-priority — only one test uses it today. | low | If row-order PG divergences proliferate (see Correctness #4), a shared helper like `PgFetchSortedAsync<T, TKey>(IPreparedQuery<T>, Func<T, TKey>)` would centralise the rationale comment. |
| 14 | Comments referencing #267 (`CrossDialectCompositionTests.cs:452-459`) and #268 (`CrossDialectCteTests.cs:221-230`) are detailed enough for a future reader: each cites the SQLSTATE / exception, names the divergence, names the workaround condition, and names the issue number. | info | Future re-enable / revert paths are documented. |
| 15 | Phase 9 increases the Pg execution surface ~75x relative to the four focused integration tests in Phase 5. Each `CrossDialect*Tests.cs` Pg block exercises Npgsql parameter binding through the real provider, so any future regression in `SqlFormatting.GetParameterName(PG, ...)` or `CarrierEmitter.FormatParamName(PG, ...)` will fail loudly across many tests, not just the four `[NpgsqlIntegration]`-categorised tests. This indirectly mitigates session-1 finding #14 (no runtime guard against `GetParameterName(PG) != ""` when `[NpgsqlIntegration]` is not run). | info | Notable test-quality upside of Phase 9: the regression surface is now broad. |

## Codebase Consistency

| # | Finding | Severity | Why It Matters |
|---|---------|----------|----------------|
| 16 | Pg result variable naming is consistent: `pgResults` for `ExecuteFetchAllAsync`, `pgResult` for `ExecuteFetchFirstAsync`/`ExecuteFetchSingleAsync`/`ExecuteFetchFirstOrDefaultAsync`, `pgAffected` for `ExecuteNonQueryAsync`, `pgNewId` for `ExecuteScalarAsync<int>` (insert-returning-id), `pg` for the prepared chain. Sampled across `CrossDialectInsertTests.cs`, `CrossDialectUpdateTests.cs`, `CrossDialectSelectTests.cs`, `CrossDialectStringOpTests.cs`, `CrossDialectAggregateTests.cs`. Convention holds. | info | Naming convention is uniform. |
| 17 | `pg2` shadow-variable pattern is internally consistent: used in `CrossDialectSubqueryTests.cs` (40 sites), `CrossDialectCteTests.cs` (1 site at line 133), `CrossDialectWindowFunctionTests.cs` (1 site at line 574). Each `pg2` is method-scoped within a single `[Test]` and consistently rebuilds the chain with the projection that matches `lt`. No naming collision risk because the variable does not escape its method. | info | Pattern is uniform. |
| 18 | `PostgresTestContainer.cs` doc comments accurately reflect the Phase 9 DDL changes. The `<remarks>` block at line 222-230 lists the column-type alignment rationale (NUMERIC for decimal, BOOLEAN for bool, TIMESTAMP/TIMESTAMPTZ for date types) and the FK omission rationale ("SQLite does not enforce FKs unless PRAGMA foreign_keys = ON"). | info | Doc-as-design-record is current. |
| 19 | `workflow.md` Decisions section (entries dated `2026-04-24 (session 2)`) accurately describes what was committed: SqlTypeMapper alignment, FK drop, three triage cases. The Session Log entry for session 2 closes with "Tests green: 2996 + 201 + 117 = 3314". Verified locally — full Quarry.Tests suite passes 2996/2996 in ~20s. | info | Workflow record matches reality. |
| 20 | `PostgresTestContainer.GetContainerAsync` retains the session-1 double-checked-locking pattern unchanged. No new instance of the pattern was introduced in Phase 9. | info | No new consistency risk. |

## Integration / Breaking Changes

| # | Finding | Severity | Why It Matters |
|---|---------|----------|----------------|
| 21 | Phase 9 is test-only. `git diff 2767e77..HEAD --stat -- src/Quarry/ src/Quarry.Generator/ src/Quarry.Shared/` returns empty — no production code was modified. The only generator-emitted file touched is `src/Quarry.Tests/ManifestOutput/quarry-manifest.postgresql.md`, which is regenerated by the source generator on every build and consumed only by `src/Quarry.Tests/Generation/ManifestEmitterTests.cs:1246` for self-testing. Not consumed outside the Tests project. | info | No production-code-facing behaviour change. |
| 22 | Test runtime: full `Quarry.Tests` suite measured at 20s for 2996 tests on this dev box (the session-2 close-out claimed the same). Cross-dialect subset (477 tests, includes all Phase 9 mirrors) runs in 8s. Phase 4's "+2s across ~3000 tests" promise has not visibly regressed; the additional ~233 Pg roundtrips amortise within the existing baseline noise on a local Docker daemon. | info | Wall-clock cost of Phase 9 is in line with the documented expectation. |
| 23 | CI compatibility: no new infrastructure dependency beyond what Phase 4 already required (Docker on the test runner — `ubuntu-latest` has it by default). The increase in Pg roundtrip volume (~233 extra `ExecuteXxxAsync` per full run) is a wall-clock matter, not a feature gate. Dev-box without-Docker fallback (session-1 finding #25) still applies — `PostgresTestContainer.GetContainerAsync` catches Docker-unavailable exceptions and calls `Assert.Ignore`. | info | No new CI risk. |
| 24 | Two new session-archive issue bodies were added (`issue-mysql.md`, `issue-mssql.md`) for follow-up work tracked as #269 (MySQL) and #270 (SQL Server). These are session-dir documentation only — not consumed by the build or by any code path. | info | Documentation-only addition. |
