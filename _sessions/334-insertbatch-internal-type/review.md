# Review: 334-insertbatch-internal-type

## Classifications

| ID | Class | Rec | Sev | Section | Finding | Action Taken |
|----|-------|-----|-----|---------|---------|--------------|
| F1 | A | A | L | Plan Compliance | Step 5's planned `Window_Rank_FetchAll` shipped as `Window_RowNumber_FetchAll` with a different window function and no `PartitionBy`/descending order; deviation undocumented. | Fixed. plan.md step 5 now records the `Window_Rank`->`Window_RowNumber` deviation, why the simpler form was taken (copied from CarrierGenerationTests.cs:2950 rather than guessing the IOverClause surface), and that PartitionBy/descending remain unexercised. |
| F2 | A | A | L | Plan Compliance | Step 6's body still lists `RawSql_NonQuery` as a shape to add even though the inline deviation note above it says it was dropped; steps 3 and 7 moved superseded text into `<details>`, step 6 did not. | Fixed. Steps 5 and 6 superseded bodies moved into `<details><summary>Original step N text</summary>` blocks, matching steps 3 and 7. Step 6 also now records the UserRow DTO choice. |
| F3 | A | C | L | Correctness | Newly public `BatchInsertSqlBuilder.Build` does no argument validation: null `sqlPrefix` throws `NullReferenceException`, and `entityCount * columnsPerRow` is unchecked so a large product overflows negative and slips past the `MaxParameterCount` guard. | Fixed. `Build` now calls `ArgumentNullException.ThrowIfNull(sqlPrefix)`, rejects `entityCount <= 0` (was `== 0`) and `columnsPerRow <= 0`, and computes the product as `long` so it cannot wrap past the ceiling. `<exception>` docs updated. |
| F4 | A | C | L | Correctness | Newly public `QueryDiagnostics` constructor does no null validation; `parameters: null` yields an instance whose non-nullable `Parameters`/`AllParameters` are null. | Fixed. Constructor validates `sql`, `parameters` and `tableName` with `ArgumentNullException.ThrowIfNull`, with an `<exception>` doc. |
| F5 | B | B | M | Test Quality | `Insert(...).ToDiagnostics()` — the third `new QueryDiagnostics(...)` emission site (`CarrierEmitter.EmitCarrierInsertToDiagnosticsTerminal`, :1095), named in plan step 3 as one of the three sites the internal ctor broke — has no shape in the matrix. | Fixed. Added the `Insert_ToDiagnostics` shape, covering CarrierEmitter:1095 — the third construction site — plus a `new QueryDiagnostics(` emission expectation. |
| F6 | B | B | M | Test Quality | 11 of the 16 shapes this PR adds have no `RuntimeHelperExpectations` entry, including four sitting under a comment that asserts each one "emits a call to a helper type". | Fixed. `RuntimeHelperExpectations` generalized to `ShapeEmissionExpectations` and extended from 9 to 33 entries covering **every** shape in the matrix; new `EveryShape_HasAnEmissionExpectation` test enforces the pairing so it cannot rot. |
| F7 | A | C | M | Test Quality | `CS1729` is excluded from `AccessibilityDiagnosticIds` wholesale; a narrower rule (CS1729 whose message names a `Quarry.*` type) would classify the second, larger instance correctly without mislabelling genuine arity bugs. | Fixed. Replaced the ID-set membership test with `IsAccessibilityDiagnostic(id, message)`, which additionally classifies CS1729 as accessibility when the quoted name matches a type the Quarry assembly declares. Covered by `AccessibilityClassifier_SeparatesInternalCtorsFromArityBugs`, including the negative case (CS1729 naming a consumer type stays an arity bug). The catch-all hint is now redundant and was removed. |
| F8 | A | C | L | Test Quality | `Shape` carries a single `Terminal`, so the "an interceptor was emitted" probe never checks the `ToDiagnostics()` half of `Prepared_MultiTerminal` — the only shape exercising the prepared diagnostics path. | Fixed. `Shape` gained `AdditionalTerminals`; `AssertBindsCleanly` probes every terminal. `Prepared_MultiTerminal` declares `ToDiagnostics` as an additional terminal. |
| F9 | A | A | L | Codebase Consistency | New `src/Quarry.Generator/llm.md` text states `ThrowHelper`, `CollectionHelper`, `CollectionSqlCache`, `ParameterNames` are "all `public` + `[EditorBrowsable(Never)]`"; none of those four carries the attribute. | Fixed. The llm.md sentence no longer claims all seven carry `[EditorBrowsable(Never)]`; it names the three that do, states the four older types are plain `public`, and directs new additions to carry it. |
| F10 | A | A | L | Codebase Consistency | The rule this PR writes into `llm-testing.md` and `llm.md` ("add a `RuntimeHelperExpectations` entry" for every new shape) is stated unconditionally but is unachievable for shapes with no distinctive runtime helper, and the PR follows it for 5 of 16 new shapes. | Fixed. Both docs now ask for "the distinctive text that shape must emit" — a helper call, or an SQL/interceptor-header fragment for emitters with no helper — which every shape can honour, and all 33 entries do. |
| F11 | B | B | M | Integration / Breaking Changes | No entry added to `docs/articles/releases/release-notes-unreleased.md` for a change that is both `fixed` (two headline APIs did not compile for any external consumer) and `added` (new public API) under `llm-release.md` §2.1. | Fixed. Added an `InsertBatch` and `ToDiagnostics()` section to `docs/articles/releases/release-notes-unreleased.md` covering both fixes, the new public API with its not-supported caveat, and why no in-repo build caught it. |
| F12 | A | A | L | Integration / Breaking Changes | `MaxParameterCount` promoted to `public const` although no emitted code names it; `const` is inlined into consumer assemblies, freezing 2100 until every consumer recompiles. (Rec raised C→A: `const`→`static readonly` keeps the approved public promotion and removes the hazard in one line.) | Fixed. `public const int` -> `public static readonly int`, with a `<remarks>` recording that a public const would be inlined into consumer assemblies and freeze the value. |
| F13 | A | A | M | Integration / Breaking Changes | The `QueryDiagnostics` 23-parameter constructor is now frozen public API, and unlike `BatchInsertSqlBuilder` its doc omits the "not part of the supported API / may change without notice" disclaimer. | Fixed. Constructor `<remarks>` now carries the same "not part of Quarry supported API / may change without notice" disclaimer as `BatchInsertSqlBuilder`, and explicitly warns against binding to the signature positionally or by named argument. |

## Plan Compliance

| ID | Finding | Sev | Why It Matters |
|----|---------|-----|----------------|
| F1 | Plan step 5 specifies `Window_Rank_FetchAll` — `Sql.Rank(over => over.PartitionBy(...).OrderByDescending(...))`. The implementation ships `Window_RowNumber_FetchAll` — `Sql.RowNumber(over => over.OrderBy(o.Total))`: different window function, no `PartitionBy`, no descending sort. Step 5's "*Expanded during implementation*" note lists only `Shape_StillReachesItsRuntimeHelper` and `CollectionEnumerableContains_FetchAll`; the body below it still names the `Sql.Rank` shape. | L | Every other deviation in this plan is documented, several of them at length — this one is not, so the plan reads as satisfied when the shipped shape covers a narrower slice of the window emitter (no partitioning, no descending ordering) than the plan asked for. The next person extending the matrix will trust the plan text. |
| F2 | Plan step 6 is checked and carries an inline "*Deviation:* `RawSql_NonQuery` dropped" note, but the step body immediately below still lists `RawSql_NonQuery` as one of three shapes to add. Steps 3 and 7 handled the same situation by moving the superseded text into a `<details><summary>Original step N text</summary>` block; steps 5 and 6 left the stale text inline. | L | A checked step whose body contradicts its own deviation note is the failure mode the `<details>` convention exists to prevent. Two of the four deviating steps use the convention and two do not, so the plan cannot be read uniformly. The underlying technical finding (`RawSqlNonQueryAsync` is never intercepted) is correct and verified — `InterceptorRouter` registers only `RawSqlAsync`/`RawSqlScalarAsync`. |

Verified as accurate and justified: step 3's deviation (three `new QueryDiagnostics(...)` emission sites confirmed at `TerminalEmitHelpers.cs:615`, `CarrierEmitter.cs:1095`, `TerminalBodyEmitter.cs:519`); step 6's `Quarry.Generated` claim (`src/Quarry/build/Quarry.targets:12` appends exactly that namespace for real consumers); step 7's claim that `FromCte<T>()` needs only the non-generic `QuarryContext` (the fixture's `TestDbContext : QuarryContext` compiles the CTE shape green). Step 4's `OrderSchema` mirrors `src/Quarry.Tests/Samples/OrderSchema.cs` declaration-for-declaration as instructed. All eight checkboxes correspond to work that is present in the diff. Fixture verified green locally: 61/61 passing.

## Correctness

| ID | Finding | Sev | Why It Matters |
|----|---------|-----|----------------|
| F3 | `BatchInsertSqlBuilder.Build` is now a public entry point with no argument validation. `sqlPrefix` is dereferenced at `BatchInsertSqlBuilder.cs:59` (`sqlPrefix.Length`) with no null check, so a null argument surfaces as `NullReferenceException` rather than `ArgumentNullException`. `columnsPerRow <= 0` is accepted. `var totalParams = entityCount * columnsPerRow` (:52) is unchecked `int` arithmetic, so a product above `int.MaxValue` wraps negative and passes the `> MaxParameterCount` guard at :53, producing a `StringBuilder` capacity computation on a negative length. | L | Unreachable from generated code — `columnsPerRow` is a compile-time constant and `entityCount` is a collection count — but the whole point of this change is that the method is no longer reachable only from generated code. A public method whose contract is "generated code always passes sane values" now has callers who are not generated code, and its `<exception>` doc promises `ArgumentException` for exactly the overflow case the guard can be made to miss. |
| F4 | `QueryDiagnostics`'s constructor is now public and validates nothing. `parameters: null` propagates through `AllParameters = allParameters ?? parameters` (:58) and the `Parameters` ternary (:73-75), producing an instance whose non-nullable-typed `Parameters` and `AllParameters` are both null; `sql` and `tableName` are likewise unchecked. | L | Same reachability caveat as F3 and the same reasoning: `[EditorBrowsable(Never)]` hides the constructor from IntelliSense but does not stop anyone binding to it, and a `QueryDiagnostics` with null collections will NRE somewhere far from the construction site. Cheap to close with null checks on the three required parameters. |

Positively verified while looking for correctness problems, and clean: `Build`'s public signature is safe (`SqlDialect` resolves to the public `Quarry.SqlDialect`, and the internal `SqlFormatting` it calls appears only in the method body, so no CS0051); `QueryDiagnostics.InsertRowCount` remains `internal` but is only written by the constructor and never read by emitted code; a member-level sweep of every emitted-surface type (`ThrowHelper`, `CollectionHelper`, `ParameterNames`, `CollectionSqlCache`, `OpId`, `QueryExecutor`, `QueryLog`, `ParameterLog`) confirms no remaining `internal`/`private` member that emitted code names, corroborating the workflow's audit.

## Security

No concerns.

## Test Quality

| ID | Finding | Sev | Why It Matters |
|----|---------|-----|----------------|
| F5 | Three call sites construct `QueryDiagnostics` and all three were broken by the internal constructor — plan step 3 and the workflow both enumerate them. Two now have shapes (`Projected_ToDiagnostics` → `TerminalEmitHelpers.cs:615`, `BatchInsert_ToDiagnostics` → `TerminalBodyEmitter.cs:519`). The third, `CarrierEmitter.EmitCarrierInsertToDiagnosticsTerminal` (`:1095`), is reached only from `TerminalBodyEmitter.EmitInsertDiagnosticsTerminal` (`:414`) — i.e. `db.Users().Insert(entity).ToDiagnostics()` and its prepared variant. No shape in the matrix does that: `Insert_ScalarAsync` and `Insert_NonQuery` take the carrier execution path, not the diagnostics path. | M | The PR's stated purpose is a recurrence guard over the sites this defect touched, and one of the three named sites is left with the same coverage it had when the bug shipped. If that emitter later gains a reference to something internal — it already emits its own `DiagnosticParameter[]` literal independently of the shared helper — nothing in the repo would notice. A single `Insert_ToDiagnostics` shape closes it. |
| F6 | `RuntimeHelperExpectations` covers 5 of the 16 shapes this PR adds. Missing: all four `JoinShapes`, both `RawSqlShapes`, and — most pointedly — `Union_FetchAll`, `Prepared_MultiTerminal`, `Cte_FromCte_FetchAll` and `Window_RowNumber_FetchAll`, which sit in `RuntimeHelperShapes` under a comment reading "Each of these emits a call to a helper type that, like BatchInsertSqlBuilder, only works if it is public." | M | For those 11 shapes the only assertions are "compiles clean" and "some interceptor was emitted for the declared terminal" — which is exactly the false-positive the PR's own working notes say `Shape_StillReachesItsRuntimeHelper` caught on its first run against `CollectionContains_FetchAll`. `Prepared_MultiTerminal` is the sharpest case: it is the only shape reaching the prepared `ToDiagnostics` path, the very path this PR fixed, and nothing pins that it emits `new QueryDiagnostics(`. `Cte_FromCte_FetchAll` and `Window_RowNumber_FetchAll` would still pass if the chain silently fell back to a runtime-build plan and stopped exercising their emitters at all. |
| F7 | The workflow decision excludes `CS1729` from `AccessibilityDiagnosticIds` on the grounds that it usually signals a genuine emitter arity bug, and compensates with a hint appended to the catch-all message. The middle option is not considered: classify `CS1729` as accessibility only when the diagnostic message names a `Quarry.*` type — the same disambiguation the appended hint asks a human to perform by hand. | M | `CS1729` is the form the larger of the two defects took, and it is the form any future internal constructor on the emitted surface will take. The catch-all does fail the build, so nothing escapes — this is a diagnosis-quality gap, not a correctness hole. But the guard's whole value proposition is that the failure message names the real cause; for the constructor case it currently does not, and the workflow itself records "the filter needs revisiting". |
| F8 | `AssertBindsCleanly`'s final probe searches for `Intercepts {shape.Terminal}() call at`, and `Shape` holds exactly one terminal. `Prepared_MultiTerminal` declares `ExecuteFetchAllAsync`, so the `_ = prepared.ToDiagnostics().Sql;` statement in its body is compiled but never probed for an emitted interceptor. | L | If the prepared `ToDiagnostics()` call stopped being intercepted it would fall through to the throwing default interface member, the shape would still compile, and the matrix would stay green. Same class of blind spot the "an interceptor was emitted" probe exists to close, just at a granularity the `Shape` record cannot express. Combined with F6 (no expectation entry either), this shape currently asserts nothing about the half of it that motivated its inclusion. |

`AccessibilityGuard_DetectsAnInaccessibleType` does prove non-vacuity, and does so correctly: `CompileNonFriend` was extracted so the control and the matrix share one assembly name and one reference set (so they cannot drift apart), `ScalarConverter` is verified still `internal` in this branch, and the second assertion (`Does.Contain("ScalarConverter")`) prevents the test from passing on an unrelated CS0122 elsewhere in the probe compilation. It also fails loudly rather than silently going vacuous if `ScalarConverter` is ever promoted. The workflow's record of having watched the guard fail end-to-end (temporarily reverting `BatchInsertSqlBuilder` to `internal`) is the right standard and is worth keeping. `CS0050`/`CS0051`/`CS0053`/`CS0060` are never exercised by any test, but they are cheap breadth on a filter whose primary ID is proven, not a gap.

## Codebase Consistency

| ID | Finding | Sev | Why It Matters |
|----|---------|-----|----------------|
| F9 | The added `src/Quarry.Generator/llm.md` section states the emitted surface is "(`BatchInsertSqlBuilder`, `ThrowHelper`, `CollectionHelper`, `CollectionSqlCache`, `ParameterNames`, `QueryExecutor`, `OpId` — all `public` + `[EditorBrowsable(Never)]`)". Only `BatchInsertSqlBuilder`, `QueryExecutor` and `OpId` carry the attribute; `ThrowHelper.cs:9`, `CollectionHelper.cs:10`, `CollectionSqlCache.cs:8` and `ParameterNames.cs:9` are plain `public`. The PR's own working notes record this correctly ("Not applied consistently"), so the doc contradicts the author's own audit. | L | `llm.md` is the authoritative internals doc for this repo and this section is the one a future emitter author will read before adding a type to the emitted surface. As written it describes a convention that four of the seven named types do not follow, which either misleads (they will assume the attribute is already there) or licenses drift (they will conclude it does not matter). Either fix the sentence or add the four attributes; both are one-line changes. |
| F10 | The same PR writes, in two docs, an unconditional rule: "add a `RuntimeHelperExpectations` entry naming the runtime member that shape is supposed to emit" (`llm-testing.md`) / "add a `RuntimeHelperExpectations` entry pinning the helper the shape is supposed to reach" (`src/Quarry.Generator/llm.md`). But joins, window functions, CTEs, set operations and raw SQL do not emit a distinctive `Quarry.Internal` helper, so there is no member to name for them — which is why 11 of the 16 new shapes have no entry. | L | A rule that the introducing commit follows for under a third of its own additions will not survive contact with the next contributor; it will be read as advisory and skipped. The mechanism is sound — it is the expectation *text* that needs to be generalized from "a runtime helper" to "the distinctive emitted token this shape exists to pin" (`Intercepts RawSqlAsync`, a `JOIN` fragment, an `OVER (` fragment), at which point the rule becomes something every shape can honour. |

Conventions that were followed correctly: `[EditorBrowsable(EditorBrowsableState.Never)]` placement matches `OpId.cs:9`/`QueryExecutor.cs:15` for the type promotion, and is correctly applied to the `QueryDiagnostics` *constructor* rather than the type (the type is a genuine consumer-facing result and should stay browsable). XML doc style, `<remarks>` usage and comment density match the surrounding fixture, which is unusually heavily commented by design. `CompileNonFriend` was extracted rather than duplicated, and `AccessibilityDiagnosticIds` is single-sourced across the assertion and the control — no utility duplication found.

## Integration / Breaking Changes

| ID | Finding | Sev | Why It Matters |
|----|---------|-----|----------------|
| F11 | Nothing was added to `docs/articles/releases/release-notes-unreleased.md`. Under `llm-release.md` §2.1 this change classifies as both `fixed` (a defect that made `InsertBatch` and `ToDiagnostics()` fail to compile for every consumer outside the friend list) and `added` (new public API: a public type, a public const, a public constructor). PR #313 is the precedent for staging exactly this kind of entry. | M | This is the most consumer-visible fix in the current unreleased window — two documented headline APIs (`llm.md:255` batch insert, `llm.md:299` "the primary tool for asserting generated SQL in tests") did not compile at all for anyone consuming Quarry from NuGet. Release Phase 2 derives notes from PR bodies when the staging file is silent, so it will not be lost, but the framing that matters ("if you tried `ToDiagnostics()` outside this repo it never worked; it does now") is exactly what the staging file exists to capture. Worth noting separately that `llm-release.md` refers to this file as `release-notes-next.md` throughout while the repo actually uses `release-notes-unreleased.md` — pre-existing drift, not introduced here. |
| F12 | `MaxParameterCount` was promoted from `internal const` to `public const`. A repo-wide search finds no emitted code and no generator code that names it — the only other `MaxParameterCount` is the unrelated `Quarry.Generator/IR/AssembledPlan.MaxParameterCount`. The stated justification is that `Build`'s `<exception>` doc crefs it, but XML doc `<see cref="..."/>` has no accessibility requirement, so the promotion was not forced by the fix. | L | `public const` in a shipped library is baked into every consumer assembly at their compile time, so the 2100 ceiling can never be lowered or raised without every consumer recompiling — and the value is explicitly framed as "a conservative default" in its own doc comment, i.e. something that may be tuned. `public static readonly int` gives the same discoverability with no inlining hazard. Additive and non-breaking today; it is the future change that is constrained. |
| F13 | The `QueryDiagnostics` constructor — 23 parameters, 19 optional — becomes public API. `QueryDiagnostics.cs` itself carries a "── New properties ──" divider recording that this payload has grown before and will grow again. `BatchInsertSqlBuilder`'s new `<remarks>` says "It is not part of Quarry's supported API and may change without notice"; the constructor's new `<summary>` says only "Not intended to be called directly". | M | Every future diagnostics field must now be appended as an optional trailing parameter, and renaming or reordering any existing one becomes a source-breaking change for any consumer who bound to it — including by named argument, which `[EditorBrowsable(Never)]` does nothing to discourage. The scope decision to fix this here is right (the alternative was pinning a broken headline API inside the very guard being built), and internal→public is additive with no consumer break today. The cheap mitigation is to state the same "may change without notice" disclaimer on the constructor that `BatchInsertSqlBuilder` already carries, so the intent is on the record before someone binds to it. |

Assessed and clean: all three visibility changes are strictly widening — no removals, no signature changes, no behavioural change to any existing member — so nothing here breaks an existing consumer at source or binary level. No AOT or trimming implication: `Quarry.Sample.Aot` is itself a friend assembly and unaffected, visibility does not change trimmer rooting for a non-rooted library reference, and no reflection, dynamic dispatch or new dependency is introduced. Manifest goldens are untouched, consistent with the plan's prediction. The `Quarry.Generated` addition to the fixture's `InterceptorsNamespaces` is verified faithful rather than permissive: `src/Quarry/build/Quarry.targets:12` appends exactly that namespace to every real consumer's build.

## Issues Created

None. The user reclassified every `C` recommendation to `A` — F3, F4, F7, F8 and F12 are all fixed in this branch rather than deferred, so no follow-up issues were filed.

Final classification: **10A / 3B / 0C / 0D** — all 13 findings addressed in this PR.
