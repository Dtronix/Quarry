# Review: 314-test-suite-guardrails

Scope: `git diff origin/master...HEAD -- . ':!_sessions'` (21 commits, 33 files), read against
`plan.md`, the dated `## Decisions`, and the `## Working Notes` / `## Suspend State` in
`workflow.md`. Verified locally: `dotnet build src/Quarry.Tests -c Release` → 0 errors,
`git status --porcelain` clean afterwards, i.e. the committed `ManifestOutput` goldens reproduce
byte-identically and the new CI drift step would pass on this tree.

Deviations that plan.md or the Working Notes already justify are not repeated as findings.
34 entries: **0 H / 12 M / 22 L**. Two of the L entries (F17, F34) are recorded *negative* results —
places a reviewer would expect a defect and there isn't one — kept in the tables so they are not
re-litigated.

> **Discovered during remediation:** the fix for F19 (assert the guard fixtures compile cleanly)
> immediately failed on the two `InsertBatch` shapes with `CS0122 'BatchInsertSqlBuilder' is
> inaccessible`. That is a genuine consumer-facing defect — the emitter hard-codes a call to an
> `internal` type, and every in-repo project that uses `InsertBatch` happens to hold an
> `InternalsVisibleTo` grant, so nothing had ever exercised it as an ordinary consumer would.
> Filed as **#334** and pinned by `KnownBug_Issue334_BatchInsert_ReferencesInternalType`.

## Classifications

Spot-verified on the main context before classifying: F19/F20/F21 by reading the test code, F30 by
checking `Quarry.Generator.csproj` (`InternalsVisibleTo Quarry.Tests` present, `IsPackable=true`,
`TrackingNames` the only public type besides `QuarryGenerator`).

| ID | Class | Rec | Sev | Section | Finding | Action Taken |
|---|---|---|---|---|---|---|
| F8 | A | A | M | Correctness | `set -euo pipefail` + `curl -fsS` makes an API blip red the workflow and discard the run | Fixed — issue filing moved to a separate `alert` job with `continue-on-error: true`; an API blip can no longer red the run or discard the publish. |
| F9 | A | A | M | Correctness | Manifest drift gate passes if manifests are never generated | Fixed — goldens are deleted before the build, so the gate now also proves they were regenerated; untracked goldens are caught too. |
| F7 | A | A | M | Correctness | Title-only dedupe + advancing baseline permanently silences the perf alert channel | Fixed — an already-open issue now receives a comment instead of a silent skip, so later regressions are still reported. |
| F6 | A | A | M | Correctness | Missing-series check red-locks the pipeline on a legitimate benchmark rename | Fixed — `workflow_dispatch` input `allow_missing_series` downgrades the failure to a warning so a deliberate rename can publish a new baseline. |
| F10 | A | A | M | Correctness | `gate.SignalAndWait()` with no timeout can hang CI with no diagnostic | Fixed — `SignalAndWait(BarrierTimeout)` (60s) throwing a `TimeoutException` that names thread-pool starvation as the cause. |
| F21 | A | A | M | Test Quality | `AssertNoStageRecomputedDifferently` passes vacuously when no stage is tracked | Fixed — asserts the tracked named-stage set is non-empty before checking run reasons. |
| F20 | A | A | M | Test Quality | #329 pin greps all generated trees, not the terminal's own signature | Fixed — the pin now matches the terminal’s own declaration via regex instead of grepping all generated trees. |
| F19 | A | A | M | Test Quality | Guard matrix never asserts the fixture compilation is otherwise error-free | Fixed — added an error-free-compilation assertion. **It immediately found a real consumer-facing bug, filed as #334** (see note above). |
| F30 | A | A | L | Consistency | `TrackingNames` is `public` in a packable assembly despite `InternalsVisibleTo` | Fixed — `TrackingNames` is now `internal`; tests reach it through the existing `InternalsVisibleTo`. |
| F33 | A | A | L | Integration | Same as F30 — the branch's only public API surface change | Fixed by F30 — the branch adds no public API surface. |
| F1 | B | B | M | Plan Compliance | Equality coverage narrower than step 4 promised; several compared fields unvaried | Fixed — added per-field negative equality tests plus a positive baseline for the fields no whole-graph test varied. |
| F29 | B | B | M | Consistency | Three `BuildReferences()` copies, already diverged on a semantics-affecting reference | Fixed — single `Testing/GeneratorTestReferences.cs`; all three fixtures share it, so the missing reference no longer differs between them. |
| F18 | B | B | M | Test Quality | Guard `<remarks>` reads as though the diagnostics are load-bearing for entity terminals | Fixed — the `<remarks>` now states which assertion is load-bearing for which shape, and that the diagnostic filter is inert for entity terminals. |
| F32 | B | B | L | Integration | Unsuppressing CS9177 is not a gate without `WarningsAsErrors` | Fixed — `<WarningsAsErrors>$(WarningsAsErrors);CS9177;CS9144</WarningsAsErrors>` on `Quarry.Tests.csproj`. |
| F14 | B | B | L | Correctness | No `.gitattributes`; CRLF checkouts make the new golden gate red for everyone | Fixed — added `.gitattributes` marking the goldens `text eol=lf`. |
| F12 | B | B | L | Correctness | `FindNode` can throw on an out-of-range stale span, re-breaking the crash-recovery path | Fixed — bound-checks the span against the root `FullSpan` and requires matching text, not just `RawKind`, before adopting a recovered node. |
| F11 | B | B | L | Correctness | Partial harness-creation failure leaks connections and open transactions | Fixed — creation disposes what it built if one harness throws; disposal continues past a throwing harness and rethrows as an `AggregateException`. |
| F13 | B | B | L | Correctness | Issue-lookup dedupe unpaginated past 100 open issues | Fixed — the dedupe query is label-filtered, which bounds it without pagination. |
| F22 | B | B | L | Test Quality | Test name says "Cached", assertion pins "Modified" — invites deleting the pin | Fixed — renamed to `ModifyOneFile_OtherFilesGroupPinnedModified_TextIdentical`. |
| F24 | B | B | L | Test Quality | Pre-cancelled UPDATE non-application asserted on SQLite only | Fixed — added cancelled UPDATEs on MySQL and SQL Server; all four dialects now assert the write did not apply. |
| F23 | B | B | L | Test Quality | Parallel-dialect test asserts counts only, not values | Fixed — the worker returns the rows and the test asserts them with `Is.EquivalentTo`, not just the count. |
| F3 | B | B | L | Plan Compliance | Step 3's paired "no-op edit stays cached" assertion was dropped undocumented | Fixed — added `WhitespaceEditToUnrelatedFile_LeavesEveryStageCached`, the cached half of the pair. |
| F2 | A | C | M | Plan Compliance | CS0103 doubly-nested-lambda generator defect never got a tracking issue | Filed as **#333**; `ConcurrencyTests` `<remarks>` and `llm-testing.md` now cite it. |
| F26 | A | C | L | Test Quality | pg/my/ss sides asserting only `Has.Count` while SQLite asserts values | Fixed — value assertions added across `CrossDialectSetOperationTests`, `CrossDialectNestedSubqueryTests` and `Join_FiveTable_Select`, order-independent where the query has no top-level ORDER BY. |
| F25 | A | C | L | Test Quality | `CrossDialectDistinctOrderByTests` never executes the my/ss sides | Fixed — the `my`/`ss` sides now execute in all nine tests. |
| F31 | A | C | L | Consistency | `DisplayClassEnricher` linear tree scan is O(sites × trees) on warm rebuilds | Fixed — a `FilePath -> SyntaxTree` dictionary is built once instead of scanning per site. |
| F15 | A | C | L | Security | `issues: write` reachable from project/NuGet code on the self-hosted runner | Fixed — `issues: write` now lives on a separate `ubuntu-latest` `alert` job that consumes only the perf-gate artifact; the self-hosted benchmark job is back to `contents: read`. |
| F16 | D | C | L | Security | Token passed via `curl` argv on a persistent self-hosted runner | Dismissed by user decision — matches the pre-existing publish-step pattern; not a regression. |
| F5 | D | D | L | Plan Compliance | "#332 must appear in the PR body" — that is the PR step itself, not a defect | Dismissed — not a defect. #332 is carried into the PR body. |
| F4 | D | D | L | Plan Compliance | Hollow-shell marker substitution; both forms prove a real body | Dismissed — both markers prove a real interceptor body; noted in the PR body. |
| F27 | D | D | L | Test Quality | Mid-stream cancellation SQLite-only — documented, correctly reasoned limit | Dismissed — documented, correctly reasoned coverage limit; surfaced in the PR body. |
| F28 | D | D | L | Test Quality | 30 sites keyed on `UserName` — documented; total for the current seed | Dismissed — documented; keys are total for the current seed and the note records what would break them. |
| F17 | D | D | L | Security | Recorded negative result — no injection in the new workflow logic | Dismissed — recorded negative result, no action. |
| F34 | D | D | L | Integration | Recorded negative results — three expected breaks that are not breaks | Dismissed — recorded negative results, no action. |

## Plan Compliance

| ID | Finding | Sev | Why It Matters |
|---|---|---|---|
| F1 | Step 4 specified negative equality "when each constituent differs" for `AssembledPlan`, `CarrierPlan`, `FileInterceptorGroup`. The implementation instead does four whole-graph comparisons over real generator runs (`PipelineModelEqualityTests.cs:260-312`: WHERE literal, projection, captured-variable, source path). Fields compared by `FileInterceptorGroup.Equals` (`FileInterceptorGroup.cs:49-62`) that **no** test varies: `ContextClassName`, `ContextNamespace`, `Sites`, `ChainMemberSites`, `Diagnostics`; on `CarrierPlan.Equals` (`CarrierPlan.cs:90-103`): `IsEligible`, `IneligibleReason`, `MaskType`, `MaskBitCount`. Not recorded as a deviation. | M | Deleting any of those comparisons would leave the whole file green — which is precisely the shipped-`_allContexts` failure class the step exists to prevent. The coverage is narrower than the step promises and nothing says so. |
| F2 | Step 8 discovered a real generator defect — a Quarry chain inside a doubly-nested lambda emits interceptors referencing display-class locals, producing `CS0103` in `*.Interceptors.*.g.cs`. It is documented in `llm-testing.md:246` and workflow.md, and the Suspend State lists it as "candidate follow-up issue … raise at REVIEW", but **no issue was filed**, unlike #328/#329/#332. | M | A build-breaking generator limitation with no tracker and no minimal repro. The other three defects found on this branch all got issue numbers and pins; this one is only findable by grepping a docs bullet. |
| F3 | Step 3 specified a schema-edit test *plus* "a no-op whitespace edit to an unrelated file leaves everything cached". `SchemaOnlyEdit_InvalidatesRegistry_RegeneratesEntities` (`IncrementalCachingTests.cs:350-392`) implements only the invalidation half; the paired no-op-edit assertion does not exist and no deviation is recorded. | L | The invalidation direction (registry barrier fires) is the easy one; the cached direction is where a false invalidation hides, and that is the half that was dropped. |
| F4 | Step 3 specified the hollow-shell guard assert `Does.Contain("file sealed class Chain_")`; `GetRealInterceptorTexts` asserts `InterceptsLocation` + `SELECT` instead (`IncrementalCachingTests.cs:147-150`). Undocumented substitution. | L | Defensible (both prove a real body), but `SELECT` also matches a comment, and the plan's marker was structural. Worth one line in the PR body rather than a silent swap. |
| F5 | `workflow.md` Suspend State requires "#332 filed during step 13b … must appear in the PR body"; `workflow.md` `pr:` is still empty and no PR exists on the branch. | L | Nine knowingly-flaky `CrossDialectJoinTests` were left unchanged by user decision. If that does not reach the PR body, the next sweep hits the trap the `<remarks>` block at `CrossDialectJoinTests.cs:9-19` was written to prevent. |

## Correctness

| ID | Finding | Sev | Why It Matters |
|---|---|---|---|
| F6 | `benchmark.yml:244-251` fails the workflow when any series present in the last published run is absent now — and the gate runs **before** the publish steps (`:253-404`), so the baseline never advances. There is no allowlist, no override input, and no "expected removals" file. | M | A legitimate benchmark rename or removal red-locks the pipeline permanently: every subsequent run sees the old name missing, fails, and refuses to publish a new baseline. The only escape is hand-editing `data.js` in the separate `Quarry-benchmarks` repo. The Decision that a missing series must fail is sound; the missing part is the way out. |
| F7 | `benchmark.yml:211-232` dedupes on the issue title alone, while every successful run publishes its results as the new baseline (`:326-364`). | M | Once one `Performance regression detected in benchmarks` issue is open, no further issue is filed for any subsequent, unrelated regression — and because the baseline advanced, the next run no longer detects the original one either. Details survive only in the job summary. Combined with the `any allocation increase` threshold (`:172`), a 1-byte BenchmarkDotNet jitter can be the event that consumes the alert channel. |
| F8 | `benchmark.yml:216-242` runs `set -euo pipefail` with `curl -fsS` for both the dedupe lookup and the POST. A GitHub API blip (5xx, rate limit) fails the step. Because the following steps carry a bare `if:` (implicit `success()`), that failure also skips `Fail on missing benchmark series`, `Generate merged HTML report`, and both publish steps. | M | Directly contradicts the 2026-07-22 decision that the perf gate is *alert-only*: a transient API error turns a clean benchmark run into a red workflow **and** discards the run (no report, no baseline update). `continue-on-error: true` on that step restores the intent. |
| F9 | `ci.yml:45-48` runs `git diff --exit-code -- src/Quarry.Tests/ManifestOutput`. If the generator ever stops emitting manifests (property renamed, `Quarry.Generator.props` import broken, emitter early-returns at `ManifestEmitter.cs:36/51`), the diff is empty and the step passes green. `git diff` also ignores untracked files, so a *newly added* golden escapes entirely. | M | This is the whole of deliverable F6, and as written it cannot distinguish "goldens verified current" from "goldens never regenerated" — the exact defect class this branch was chartered to remove. Assert the four files exist and were rewritten during the build (or use `git status --porcelain -- <path>` and check emptiness of that *plus* a freshness probe). |
| F10 | `ConcurrencyTests.cs:155` calls `gate.SignalAndWait()` with no timeout, from 8 `Task.Run` bodies (`:183-190`). All 8 block a thread-pool thread until the last arrives. | M | On a 2-core runner only ~2 threads start immediately; the rest depend on thread-pool injection (~1/sec). If injection is throttled or another fixture is saturating the pool, the test hangs indefinitely with no assertion message — a CI timeout with no diagnostic. Use `SignalAndWait(TimeSpan.FromSeconds(30))` and fail loudly, or create the workers with `TaskCreationOptions.LongRunning`. |
| F11 | `var harnesses = await CreateHarnessesAsync(Workers);` sits **outside** the `try` in all three tests (`ConcurrencyTests.cs:113`, `:180`, `:244`), and `CreateHarnessesAsync` (`:46-52`) creates them in a loop with no rollback. `DisposeAllAsync` (`:54-61`) also aborts on the first throwing dispose, skipping the remainder. | L | A failure creating harness 5 of 8 leaks 4 harnesses — each holding an open SQLite connection plus open PG/MySQL/SQL Server transactions against the *shared* baseline — for the rest of the process, which will cascade into lock timeouts in later fixtures. `QueryTestHarness.CreateAsync` itself goes to real trouble to roll back partial setup (`QueryTestHarness.cs:120-131`); this loop discards that discipline. |
| F12 | `DisplayClassEnricher.cs:109-113` recovers a stale node with `currentTree.GetRoot().FindNode(lambda.Span, …)` and accepts it if `RawKind` matches. `FindNode` throws `ArgumentOutOfRangeException` when the span is not inside the root's `FullSpan`. | L | The recovery path exists precisely to stop an exception from killing the whole generator run (CS8785, all interceptors vanish); if the recovered tree is shorter than the stale span, it reintroduces the same failure. `RawCallSite.Equals` compares `Line`/`Column`/`InterceptableLocationData` (`RawCallSite.cs:459-467`), so a stale-but-shifted span is unlikely — but "unlikely" is what the original crash was too. Bound-check the span (or wrap in try/catch) and validate more than `RawKind` before adopting the node, since a same-span same-kind *different* lambda would be silently enriched with the wrong closure. |
| F13 | `benchmark.yml:224` looks for an existing issue via `issues?state=open&per_page=100` with no pagination. | L | Past 100 open issues the dedupe silently stops working and a new issue is filed on every run. `labels=performance-regression` in the query, or the search API, bounds it. |
| F14 | The repo has no `.gitattributes`; the goldens are stored LF in the index, but `ManifestEmitter` builds them with 32 `AppendLine` calls (`Environment.NewLine`) and writes via `File.WriteAllText` (`ManifestEmitter.cs:698-712`). | L | Correct on CI (ubuntu, LF) and on a Windows box with `core.autocrlf=true` — but a Windows contributor with `autocrlf=false` gets all four goldens permanently dirty and, if they commit that, turns the new `ci.yml` step red for everyone. Adding `*.md text eol=lf` (or emitting `\n` explicitly) makes the new gate platform-independent. |

## Security

| ID | Finding | Sev | Why It Matters |
|---|---|---|---|
| F15 | `benchmark.yml:71-73` grants `issues: write` to the `benchmark` job — the same job that runs `dotnet run --project src/Quarry.Benchmarks` (`:86-89`) and `dotnet run --project src/Quarry.Benchmarks.Reporter` (`:253-262`) on the persistent self-hosted `debian-benchmark` runner. | L | The scope itself is minimal and correct for the feature, but it is now reachable from arbitrary project + NuGet code executing in that job. Trigger surface is limited (`workflow_run` on master + `workflow_dispatch`, no fork PRs), so this is defence-in-depth, not an exposure: filing the issue from a separate `ubuntu-latest` job that only consumes `perf-gate.md` as an artifact would keep the build job read-only. |
| F16 | `benchmark.yml:222` and `:239` pass the token as a `curl` argv header (`-H "Authorization: Bearer ${GH_TOKEN}"`) on a persistent self-hosted runner, where `/proc/*/cmdline` is readable by co-tenant processes. | L | Matches the pre-existing pattern of the publish step (`:309`, which carries the more powerful `BENCHMARK_TOKEN`), so this is not a regression — but the new steps extend it to `GITHUB_TOKEN`. `--config` / `-H @-` from stdin avoids argv entirely. |
| F17 | No injection defect found in the new workflow logic, and this was checked specifically. Values crossing the shell boundary go through `jq --arg` / `--argjson` / `--rawfile` (`:152`, `:234`), which parse rather than interpolate; `$(git log -1 --format='%s')` at `:182` lands inside a double-quoted `echo` whose output is never re-evaluated; the only interpolated identifier is `${GITHUB_REPOSITORY}` (trusted). `prev-data.js` is fetched over HTTPS and only reaches `jq` as `--argjson`, so a malformed/hostile baseline can at worst fail the step. | L | Recorded so the negative result is on the record rather than assumed. The residual issue is presentational only: benchmark names and the commit subject flow verbatim into an issue body, so `@mentions` in a commit subject would notify. |

## Test Quality

| ID | Finding | Sev | Why It Matters |
|---|---|---|---|
| F18 | `InterceptorBindingGuardTests.cs:200-205` asserts the absence of `CS9144`/`CS9177` across 22 shape runs — but the step-7 Working Note establishes that an isolated `CSharpCompilation` **does not raise CS9144 for a wrong-arity terminal receiver at all** ("a hand-written `[InterceptsLocation]` interceptor with a deliberately wrong receiver arity also produces no CS9144"). The cited bite-verification (`CarrierEmitter.cs:250` mutation) broke the *whole project* build and was never observed firing inside this harness. | M | For the 9 entity-terminal shapes — the shapes the fixture exists for — the headline assertion is inert; the only load-bearing check is the `Does.Contain($"Intercepts {shape.Terminal}() call at")` text probe at `:211`. The Working Notes are honest about this, but the test's own `<remarks>` (`:24-32`) reads as though the diagnostics are the guard. The reasoning that the matrix is non-vacuous holds only for *clause* shapes, and only outside this harness. |
| F19 | `AssertBindsCleanly` (`InterceptorBindingGuardTests.cs:191-214`) never asserts that the fixture compilation is otherwise error-free — it filters `outputCompilation.GetDiagnostics()` down to `CS8785`/`CS9144`/`CS9177` only. | M | The reference set is deliberately minimal (`:300-324`). If a future fixture edit introduces an unrelated `CS0246`/`CS1061`, the compiler stops validating `[InterceptsLocation]` bindings and every shape passes green. One extra `Assert.That(diagnostics.Where(d => d.Severity == Error), Is.Empty)` closes it and costs nothing. |
| F20 | `KnownBug_Issue329_EntityTerminal_EmitsTwoArityReceiver` (`:236-245`) asserts `Does.Contain("this IQueryBuilder<User, User> builder")` against `string.Concat(generatedSources)` — the concatenation of **every** generated tree in the compilation, not the signature of `shape.Terminal`. | M | A bug pin must fail exactly when the bug is fixed. This one passes if *any* emitted method anywhere in the run carries a two-arity receiver, so after #329 is fixed it could stay green on an unrelated emission and the `.Select(...)` workarounds in the three integration suites would outlive their cause. Scope the assertion to the terminal's own emitted signature (e.g. `$"{shape.Terminal}_"` on the same line, or match against the single tree containing the terminal). |
| F21 | `AssertNoStageRecomputedDifferently` (`IncrementalCachingTests.cs:171-197`) iterates `TrackedSteps.Where(kv => namedStages.Contains(kv.Key))` and applies `Is.All.Matches`, which passes on an empty sequence. `UnchangedSource_FreshlyParsedTrees_AllStagesCached` (`:227-251`) therefore never asserts that a single named stage was actually evaluated. | M | "An absent stage is a cached signal" is a correct and documented Roslyn fact, but it makes this helper unable to distinguish "all seven stages compared equal" from "no stage was tracked at all". The sibling `StageOutputs` (`:156-163`) does guard stage presence; this one — the flagship assertion for finding F1, the review's only H item — does not. Assert the observed stage set is a subset of `namedStages` *and* non-empty, or that each named stage is either present-and-cached or provably skipped. |
| F22 | `ModifyOneFile_OtherFilesGroupCached_TextIdentical` (`IncrementalCachingTests.cs:254`) is named for a cached group but asserts `Is.EqualTo(Modified)` at `:296` (the #310 pin). | L | The name and the assertion disagree. The next person to read this will "fix" the assertion to match the name and silently delete the pin. Rename to `..._OtherFilesGroupPinnedModified_TextIdentical`. |
| F23 | `ParallelContexts_SeparateConnections_AllDialectsQueryConcurrently` stores only counts in `DialectCounts` (`ConcurrencyTests.cs:214`) and asserts only counts (`:256-267`). | L | The declared purpose ("a shared parameter buffer would cross dialects as well as workers", `:220-221`) is only partly served: a bleed that swapped `threshold` between workers is caught (3 vs 2), but any cross-talk that returns the wrong *rows* at the right cardinality is invisible. The sibling mixed read/write test asserts values; this one should too. |
| F24 | `PreCancelledToken_EveryFetchTerminal_ThrowsOperationCanceled` verifies the cancelled UPDATE did not apply only on SQLite (`CancellationTests.cs:133`); the PostgreSQL cancelled UPDATE at `:112-114` has no corresponding `Does.Not.Contain("cancelled")`. | L | The interesting half of "pre-cancelled" is that nothing executed. It is asserted for one of the four dialects. |
| F25 | `CrossDialectDistinctOrderByTests` prepares and SQL-asserts all four dialects but executes only `lt` and `pg` in all nine tests (`:61-64, 108-111, 159-162, 210-216, 269-272, 320-323, 394-397, 438-441, 491-494`). | L | Carried into REVIEW by the Suspend State and still unaddressed/unfiled. Pre-existing, not introduced here, but it means the DISTINCT+ORDER BY execution semantics are unverified on MySQL and SQL Server — the two dialects where the derived-table wrap differs most. |
| F26 | Pg/My/Ss sides that assert only `Has.Count` while the SQLite side asserts values — `Join_FiveTable_Select`, ~11 `CrossDialectSetOperationTests`, 8 of 9 pg sites in `CrossDialectDistinctOrderByTests`, two `CrossDialectNestedSubqueryTests`. | L | Listed in the Suspend State as a candidate issue; no issue filed. Immune to row-order flake (correctly skipped by the sweep rules) but blind to a wrong-rows regression on the real providers — the sweep hardened the tests that *had* value assertions and left the ones that never had any. |
| F27 | Mid-stream cancellation asserts `OperationCanceledException` on SQLite only (`CancellationTests.cs:202-243`); the all-dialect test asserts connection usability instead (`:148-184`). | L | A documented, correctly reasoned limit (a provider that has buffered the result set never awaits I/O again), recorded here per the Suspend State so it is visible in the PR rather than only in workflow.md. Not a defect — but it does mean PG/MySQL/SQL Server cancellation propagation is untested, and the `when (ex is not OperationCanceledException)` filters in those paths remain unexercised. |
| F28 | 30 converted sweep sites key on `UserName` (`CrossDialectSubqueryTests.cs:396-646`, `CrossDialectHasManyThroughTests.cs:219-232`, `CrossDialectJoinTests.cs:141-153, 521-533, 963-975`). Total only because the seed has three distinct user names. | L | Documented in the step-11 Working Notes; recorded here because the key is not derivable from the projection alone — a second "Alice" in the seed silently reintroduces the flake with a stable sort that preserves the provider's arbitrary order. All other converted sites use a primary key and are total by construction; spot-checked keys (`UserId`, `OrderId`, `ProductId`, `Id`, `Item1`, `s => s`) are sound, and no site with a top-level `ORDER BY` was wrapped (verified by scanning every converted test). |

## Codebase Consistency

| ID | Finding | Sev | Why It Matters |
|---|---|---|---|
| F29 | Three near-identical `BuildReferences()` helpers were written rather than one shared fixture: `IncrementalCachingTests.cs:75-95`, `PipelineModelEqualityTests.cs:162-182`, `InterceptorBindingGuardTests.cs:300-324`. They have **already diverged** — only the third includes `System.ComponentModel.Primitives.dll`. The `SharedSource` schema fixture is likewise duplicated verbatim between the first two. | M | The step-6 Working Note records that missing that exact reference "degrades semantics enough to flip generator classification (identity-projection fallback)". So the two older copies run the generator under measurably different semantics from the newest one, and the divergence is invisible at the call sites. A single `GeneratorTestReferences` helper (with the reason for each reference in one place) removes a whole class of future confusion. |
| F30 | `TrackingNames` is declared `public` (`TrackingNames.cs:9`) although `Quarry.Generator.csproj:35` already declares `<InternalsVisibleTo Include="Quarry.Tests" />`. Every other type in the generator assembly except `QuarryGenerator` itself is `internal` — including `FileInterceptorGroup`, which the same test file casts to (`IncrementalCachingTests.cs:279`) via that very `InternalsVisibleTo`. | L | Adds a type to the public surface of a shipped NuGet package (`Quarry.Generator`, `IsPackable=true`) for a test-only concern, when the mechanism to avoid it is already wired up and already used two lines away. |
| F31 | `DisplayClassEnricher.cs:99-107` does a linear scan of `compilation.SyntaxTrees` for every stale site, inside a loop that otherwise caches carefully (`semanticModelCache` at `:75`, `methodAnalysisCache` at `:79`). | L | O(sites × trees) on a path that fires for *every* site on a warm-rebuild run of a large project. A one-time `FilePath → SyntaxTree` dictionary matches the file's own idiom and removes the quadratic. |

## Integration / Breaking Changes

| ID | Finding | Sev | Why It Matters |
|---|---|---|---|
| F32 | Removing `<NoWarn>$(NoWarn);CS9177</NoWarn>` from `Quarry.Tests.csproj` is correct and evidence-backed (2026-07-23 decision; a full build with the suppression overridden reported zero CS9177). But no project in the repo sets `TreatWarningsAsErrors`/`WarningsAsErrors` (checked repo-wide), so unsuppressing the diagnostic is not itself a gate — a future CS9177 would surface as a build warning nobody fails on. | L | Consumer impact: none (test project only, no package). The real regression net is `InterceptorBindingGuardTests`, subject to F18/F19 above. If the intent is that CS9177 must never reappear, it needs `<WarningsAsErrors>CS9177;CS9144</WarningsAsErrors>` on that project, not merely the absence of NoWarn. |
| F33 | New public type in the shipped `Quarry.Generator` package — see F30. | L | The only public API surface change on the branch. Not breaking; additive and reversible if made `internal` before release. |
| F34 | Verified non-breaks, recorded so they are not re-litigated: (a) the job-level `permissions:` block added at `benchmark.yml:71-73` does **not** break publishing — `Ensure gh-pages branch exists` and `Publish benchmark run` both authenticate with `secrets.BENCHMARK_TOKEN` (`:286`, `:404`), not `GITHUB_TOKEN`; (b) the six query-side `ORDER BY` additions changed pinned SQL on all four dialects and regenerated all four goldens, and a local `dotnet build` reproduces them byte-identically (`git status` clean); (c) no package references were added or bumped. | L | These are the three places a reviewer would expect a break and there isn't one. |

## Issues Created

- #332: Nine `CrossDialectJoinTests` assert row order that no client-side sort can reproduce
  *(filed during step 13b, by user decision, instead of changing the tests)*
- #333: Chains inside doubly-nested lambdas emit interceptors that fail to compile (CS0103 on captured locals)
- #334: `InsertBatch` interceptors reference internal `BatchInsertSqlBuilder` — CS0122 for any
  consumer outside `InternalsVisibleTo` *(found by the F19 fix during remediation)*
