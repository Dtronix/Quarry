namespace Quarry.Generators.Generation;

internal static partial class InterceptorCodeGenerator
{
    // Per-site interceptor dispatch moved to CodeGen.FileEmitter.EmitInterceptorMethod
    // Clause body emission moved to CodeGen.ClauseBodyEmitter
}
